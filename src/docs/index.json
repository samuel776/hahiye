{
  "swagger": "2.0",
  "info": {
    "description": "Documentation for Hahiye API",
    "version": "1.0.0",
    "title": "Hahiye Backend"
  },
  "basePath": "/api",
  "tags": [
    {
      "name": "user",
      "description": "Operations related to authentication"
    }
  ],
  "schemes": ["http", "https"],
  "paths": {
    "/users/login": {
      "post": {
        "tags": ["users"],
        "summary": "Login a user with email & password",
        "consumes": ["application/json"],
        "produces": ["application/json"],
        "parameters": [
          {
            "in": "body",
            "name": "body",
            "schema": {
              "$ref": "#/definitions/Login"
            },
            "required": true
          }
        ],
        "responses": {
          "200": {
            "description": "Login successfully",
            "schema": {
              "$ref": "#/definitions/LoginResponse"
            }
          },
          "401": {
            "description": "Wrong credentials (password)"
          }
        }
      }
    },
    "/users/signup": {
      "post": {
        "tags": ["users"],
        "summary": "Signup user",
        "description": "Provide proper information to create account",
        "operationId": "registerUser",
        "consumes": ["application/json"],
        "produces": ["application/json"],
        "parameters": [
          {
            "in": "body",
            "name": "body",
            "schema": {
              "$ref": "#/definitions/Signup"
            },
            "required": true
          }
        ],

        "responses": {
          "201": {
            "description": "User created successfully ",
            "schema": {
              "$ref": "#/definitions/LoginResponse"
            }
          },
          "400": {
            "description": "Operation failed"
          }
        }
      }
    },

    "/users/forgotPassword": {
      "post": {
        "tags": ["users"],
        "summary": "forgetting and resetting password",
        "consumes": ["application/json"],
        "produces": ["application/json"],
        "parameters": [
          {
            "in": "body",
            "name": "body",
            "schema": {
              "$ref": "#/definitions/forgotPassword"
            },
            "required": true
          }
        ],
        "responses": {
          "200": {
            "description": "Email has been sent, kindly follow the instructions to reset your password",
            "schema": {
              "$ref": "#/definitions/ForgotPassword"
            }
          },
          "500": {
            "description": "something went wrong"
          }
        }
      }
    },

    "/users/resetPassword/:resetLink": {
      "post": {
        "tags": ["users"],
        "summary": "forgetting and resetting password",
        "consumes": ["application/json"],
        "produces": ["application/json"],
        "parameters": [
          {
            "in": "body",
            "name": "body",
            "schema": {
              "$ref": "#/definitions/resetPassword"
            },
            "required": true
          }
        ],
        "responses": {
          "200": {
            "description": "your password has been changed successfully",
            "schema": {
              "$ref": "#/definitions/resetPassword"
            }
          },
          "500": {
            "description": "something went wrong while updating user"
          }
        }
      }
    }
  },
  "definitions": {
    "Login": {
      "type": "object",
      "required": ["user_email", "user_password"],
      "properties": {
        "user_email": {
          "type": "string"
        },
        "user_password": {
          "type": "string"
        }
      },
      "responses": {
        "201": {
          "description": "User created successfully ",
          "schema": {
            "$ref": "#/definitions/LoginResponse"
          }
        },
        "400": {
          "description": "Operation failed"
        }
      }
    },
    "forgotPassword": {
      "type": "object",
      "required": "user_email",
      "properties": {
        "user_email": {
          "type": "string"
        }
      },
      "responses": {
        "200": {
          "description": "Email has been sent, kindly follow the instructions to reset your password "
        },
        "500": {
          "description": "something went wrong "
        }
      }
    },
    "resetPassword": {
      "type": "object",
      "required": "newPass",
      "properties": {
        "newPass": {
          "type": "string"
        }
      },
      "responses": {
        "200": {
          "description": "your password has been changed successfully "
        },
        "500": {
          "description": "something went wrong while updating use"
        }
      }
    },

    "Signup": {
      "type": "object",
      "required": ["user_email", "user_password", "user_name"],
      "properties": {
        "user_name": {
          "type": "string"
        },
        "user_email": {
          "type": "string"
        },
        "user_password": {
          "type": "string"
        },
        "confirm_password": {
          "type": "string"
        }
      }
    },
    "LoginResponse": {
      "type": "object",
      "required": ["success", "status"],
      "properties": {
        "success": {
          "type": "boolean"
        },
        "status": {
          "type": "integer"
        },
        "message": {
          "type": "string"
        },
        "data": {
          "type": "string"
        }
      },
      "xml": {
        "name": "LoginResponse"
      }
    }
  }
}
